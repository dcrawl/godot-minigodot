# Scene Plan (Placeholder)

Create a simple scene for attach/execute validation when MiniScript Script resource support is live.

Suggested shape:

- Root: Node
- Attached script: `hello.ms`
- Entry point: equivalent of ready/initialization callback
